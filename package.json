{
  "name": "@shrwnsan/weather-skill",
  "version": "0.3.2",
  "description": "A platform-agnostic weather skill for AI agents (Bun/TypeScript runtime).",
  "type": "module",
  "engines": {
    "bun": ">=1.1.30"
  },
  "main": "src/index.ts",
  "bin": {
    "weather": "src/cli.ts"
  },
  "files": [
    "src/",
    "weather/data/"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/cli.ts --compile --target=bun-linux-x64 --outfile weather-linux-x64",
    "build:linux-x64": "bun build src/cli.ts --compile --target=bun-linux-x64 --outfile weather-linux-x64",
    "build:darwin-arm64": "bun build src/cli.ts --compile --target=bun-darwin-arm64 --outfile weather-darwin-arm64",
    "build:all": "bun run build:linux-x64 && bun run build:darwin-arm64"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shrwnsan/weather-skill.git"
  },
  "homepage": "https://github.com/shrwnsan/weather-skill",
  "bugs": {
    "url": "https://github.com/shrwnsan/weather-skill/issues"
  },
  "license": "MIT"
}
